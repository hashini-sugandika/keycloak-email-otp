# Unique key for the project in SonarQube (use lowercase, no spaces)
sonar.projectKey=keycloak-email-otp

# Display name in SonarQube UI
sonar.projectName=Keycloak Email OTP

# Version (you can keep it in sync with your code versioning)
sonar.projectVersion=1.0

# The source encoding
sonar.sourceEncoding=UTF-8

# Paths to source code (relative to the root)
sonar.sources=src/main/java

# If you have test sources
#sonar.tests=src/main/java/com/weare5stones/keycloak/authenticators/emailotp/EmailOTPAuthenticator.java

# If you want to include or exclude specific files or directories
# sonar.exclusions=**/some_dir_to_exclude/**

# URL of your SonarQube server
sonar.host.url=http://localhost:9000

# Authentication token generated by SonarQube (replace with your token)
sonar.login=sqp_d62f2df76e4c4f3ff1a490488cff372acafd0f1f

# If your project uses Maven, you can optionally provide the pom
# sonar.java.binaries=target/classes
